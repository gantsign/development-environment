---
- name: Install ctop
  become: true
  become_user: vagrant
  community.general.homebrew:
    name: ctop
    state: present
  environment:
    HOMEBREW_INSTALL_FROM_API: '1'
  tags:
    - homebrew
