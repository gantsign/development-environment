---
- name: Install Docker CLI tools (Homebrew)
  become: true
  become_user: vagrant
  community.general.homebrew:
    name:
      - ctop
      - dive
    state: present
  environment:
    HOMEBREW_INSTALL_FROM_API: '1'
