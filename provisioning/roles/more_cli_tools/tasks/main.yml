---
- name: Install more CLI tools (Homebrew)
  become: true
  become_user: '{{ my_user }}'
  community.general.homebrew:
    name:
      - bandwhich
      - dust
      - gh
      - git-delta
      - lsd
      - procs
      - sd
      - starship
      - tokei
      - watchexec
      - yq
    state: present
  environment:
    HOMEBREW_INSTALL_FROM_API: '1'
